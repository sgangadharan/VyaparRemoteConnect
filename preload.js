// preload.js

// This file runs before the renderer (web page) is loaded.
// You can expose safe APIs to the renderer here if needed.

window.addEventListener('DOMContentLoaded', () => {
    console.log('Preload script loaded');
  });